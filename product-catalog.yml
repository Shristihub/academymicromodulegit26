spring:
#datasource properties
  datasource:
    username: root
    password: root
    url: jdbc:mysql://mysql-container:3306/ekartdb
#jpa specific properties
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false 
message: Catalog of products for diff categories
